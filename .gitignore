# build tool sources (not part of the assignment deliverable)
ngspice/
ngspice-*.tar.gz
autoconf-*/
autoconf-*.tar.gz
*.o
*.tmp

# large raw simulation output (keep plots/summaries, not every raw csv, unless small)

# CACTI source/build (tool dependency, not part of the assignment deliverable)
cacti-src/

# NVSim source/build (tool dependency, not part of the assignment deliverable)
nvsim-src/

# Ramulator2 source/build (tool dependency, not part of the assignment deliverable)
ramulator2-src/

# gem5 source/build (tool dependency, not part of the assignment deliverable)
gem5-src/

# gem5 source/build (tool dependency, not part of the assignment deliverable)
gem5-src/
